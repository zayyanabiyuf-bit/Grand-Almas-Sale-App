{
  "name": "grand-almas-sales-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting needed for vanilla JS'"
  },
  "dependencies": {
    "vite": "^6.2.0"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
